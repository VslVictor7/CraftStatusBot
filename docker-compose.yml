services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bot-desenvolvimento
    env_file:
      - .env
    tty: true
    stdin_open: true
    volumes:
      - /data/logs/latest.log:/logs/latest.log:ro
      - /data/world/stats:/world/stats:ro
      - ./players.json:/app/bot/core/utils/json/players.json:ro # Only map it if SERVER_MODE=1
    networks:
      - request

networks:
  request:
    external: true